# ميزة عرض الحضور والغياب للطالب 📅

## نظرة عامة

تم إضافة ميزة عرض الحضور والغياب بشكل كامل مع واجهة مستخدم جميلة ومتكاملة مع الـ API. تعرض الصفحة الإحصائيات العامة والمواد الدراسية مع تفاصيل كل جلسة.

---

## 🎯 الملفات المضافة/المعدلة

### 1. **الملفات الجديدة**
- `src/types/attendance.ts` - أنواع TypeScript للحضور
- `src/services/attendanceService.ts` - خدمة API للحضور
- `src/screens/AttendanceScreen.tsx` - صفحة عرض الحضور

### 2. **الملفات المعدلة**
- `src/services/apiConfig.ts` - إضافة endpoint الحضور
- `src/screens/HomeScreen.tsx` - إضافة زر الحضور
- `src/navigation/AppNavigator.tsx` - ربط صفحة الحضور بالتنقل

---

## 🔗 API Endpoint المستخدم

```typescript
ATTENDANCE_RECORDS: '/api/trainee-auth/attendance-records'
```

---

## 📊 بنية البيانات

### **Response Structure**
```typescript
{
  trainee: {
    id: number;
    nameAr: string;           // الاسم بالعربية
    nameEn: string;           // الاسم بالإنجليزية
    nationalId: string;       // الرقم القومي
    photoUrl: string | null;  // رابط الصورة الشخصية
    program: {
      id: number;
      nameAr: string;         // اسم البرنامج بالعربية
      nameEn: string;         // اسم البرنامج بالإنجليزية
      price: number;
      description: string | null;
      numberOfClassrooms: number;
      createdAt: Date;
      updatedAt: Date;
    };
    classroom: {
      id: number;
      name: string;           // اسم الفصل الدراسي
      classNumber: number;    // رقم الفصل
      programId: number;
      startDate: Date | null;
      endDate: Date | null;
      createdAt: Date;
      updatedAt: Date;
    } | null;
  };
  
  stats: {
    total: number;            // إجمالي عدد الجلسات
    present: number;          // عدد الحضور
    absent: number;           // عدد الغياب
    late: number;             // عدد التأخير
    excused: number;          // عدد الغياب بعذر
    attendanceRate: number;   // نسبة الحضور (مئوية)
  };
  
  contentGroups: Array<{
    content: {
      id: number;
      nameAr: string;         // اسم المادة بالعربية
      nameEn: string;         // اسم المادة بالإنجليزية
    };
    
    sessions: Array<{
      id: string;             // معرف سجل الحضور
      sessionId: number;      // معرف الجلسة
      date: Date;             // تاريخ الجلسة
      dayOfWeek: "SUNDAY" | "MONDAY" | "TUESDAY" | "WEDNESDAY" | "THURSDAY" | "FRIDAY" | "SATURDAY";
      sessionType: "THEORY" | "PRACTICAL";  // نوع الجلسة (نظري/عملي)
      status: "PRESENT" | "ABSENT" | "LATE" | "EXCUSED";  // حالة الحضور
      isCancelled: boolean;   // هل الجلسة ملغاة
      notes: string | null;   // ملاحظات
      createdAt: Date;        // تاريخ إنشاء السجل
    }>;
    
    stats: {
      total: number;           // إجمالي جلسات المادة
      present: number;         // حضور في المادة
      absent: number;         // غياب في المادة
      late: number;            // تأخير في المادة
      excused: number;        // غياب بعذر في المادة
      attendanceRate: number;  // نسبة الحضور في المادة
    };
  }>;
}
```

---

## 🎨 واجهة المستخدم

### **الميزات الرئيسية**

#### 1️⃣ **الإحصائيات العامة**
```
┌─────────────────────────────────┐
│        إحصائيات الحضور          │
│         اسم الطالب              │
├─────────────────────────────────┤
│  85.5%    │   50    │   45     │
│ نسبة      │ إجمالي  │ حاضر     │
│ الحضور    │ الجلسات │          │
├─────────────────────────────────┤
│           جيد جداً              │
└─────────────────────────────────┘
```

#### 2️⃣ **المواد الدراسية**
```
┌─────────────────────────────────┐
│        الرياضيات                │
│        20 جلسة • 90.0%          │
│                        90.0%    │
│                              ▼  │
├─────────────────────────────────┤
│  ✅ 18  ❌ 1   ⏰ 1   📝 0     │
│  حاضر غائب متأخر بعذر           │
├─────────────────────────────────┤
│  سجلات الحضور:                  │
│  ┌─────────────────────────┐    │
│  │ 15 يناير 2024  الأحد    │    │
│  │ ✅ حاضر  📚 نظري        │    │
│  └─────────────────────────┘    │
│  ┌─────────────────────────┐    │
│  │ 16 يناير 2024  الاثنين  │    │
│  │ ❌ غائب  🔬 عملي        │    │
│  └─────────────────────────┘    │
└─────────────────────────────────┘
```

#### 3️⃣ **فلترة الجلسات**
- الكل
- حاضر (أخضر)
- غائب (أحمر)
- متأخر (برتقالي)
- بعذر (بنفسجي)

---

## 🎯 أنواع الحضور

### **Attendance Status**
```typescript
enum AttendanceStatus {
  PRESENT = 'PRESENT',     // حاضر
  ABSENT = 'ABSENT',       // غائب
  LATE = 'LATE',           // متأخر
  EXCUSED = 'EXCUSED',     // بعذر
}
```

### **Session Type**
```typescript
enum SessionType {
  THEORY = 'THEORY',       // نظري
  PRACTICAL = 'PRACTICAL', // عملي
}
```

### **ألوان الحضور**
- **حاضر**: أخضر `#10B981` ✅
- **غائب**: أحمر `#EF4444` ❌
- **متأخر**: برتقالي `#F59E0B` ⏰
- **بعذر**: بنفسجي `#8B5CF6` 📝

---

## 🚀 كيفية الاستخدام

### 1. الوصول لصفحة الحضور
من الشاشة الرئيسية → اضغط على زر "الحضور" 📅

### 2. عرض سجلات الحضور
- تظهر الإحصائيات العامة في الأعلى
- قائمة بالمواد الدراسية
- كل مادة تحتوي على الجلسات

### 3. تفاصيل الجلسة
1. اضغط على أي مادة لرؤية التفاصيل
2. ستظهر جميع الجلسات مع التواريخ
3. حالة الحضور لكل جلسة
4. نوع الجلسة (نظري/عملي)
5. ملاحظات إضافية

### 4. فلترة الجلسات
- استخدم الفلاتر في الأعلى
- تصفية حسب حالة الحضور
- عرض الجلسات المحددة فقط

---

## 📱 حالات الشاشة

### Loading
- عرض spinner مع رسالة "جاري تحميل سجلات الحضور..."

### Error
- عرض emoji تحذير
- رسالة الخطأ
- زر "إعادة المحاولة"

### Empty
- عرض emoji 📅
- رسالة "لا توجد سجلات حضور"

### Success
- عرض الإحصائيات العامة
- قائمة بالمواد الدراسية
- تفاصيل كل جلسة

---

## 🎨 نظام الألوان

### **الخلفيات**
```css
--bg-primary: #FFFFFF;      /* خلفية رئيسية */
--bg-secondary: #F9FAFB;    /* خلفية ثانوية */
--bg-tertiary: #F3F4F6;     /* خلفية أزرار */
```

### **النصوص**
```css
--text-primary: #1F2937;    /* نصوص رئيسية */
--text-secondary: #6B7280;  /* نصوص ثانوية */
--text-error: #EF4444;      /* نصوص خطأ */
```

### **حالات الحضور**
```css
--attendance-present: #10B981;   /* حاضر */
--attendance-absent: #EF4444;    /* غائب */
--attendance-late: #F59E0B;      /* متأخر */
--attendance-excused: #8B5CF6;   /* بعذر */
```

---

## 🔄 تدفق البيانات

```
1. المستخدم يفتح صفحة الحضور
   ↓
2. AttendanceScreen.tsx → loadAttendance()
   ↓
3. attendanceService.getAttendanceRecords(accessToken)
   ↓
4. API Request → /api/trainee-auth/attendance-records
   ↓
5. Response → AttendanceRecordsResponse
   ↓
6. تحديث state → setAttendanceData(response.data)
   ↓
7. عرض البيانات في البطاقات
```

---

## 🛠️ SOLID Principles

الكود يتبع مبادئ SOLID:
- **Single Responsibility**: كل ملف له مسؤولية واحدة
- **Open/Closed**: يمكن التوسع بدون تعديل الكود الموجود
- **Interface Segregation**: واجهات محددة لكل غرض
- **Dependency Inversion**: الاعتماد على Abstractions وليس Concretions

---

## 🧪 كيفية الاختبار

### 1. تأكد من تشغيل الخادم
```bash
# تأكد من أن API متاح على أحد الفروع
https://mansapi.tiba29.com
# أو
https://zagapi.tiba29.com
```

### 2. سجل الدخول
- استخدم حساب متدرب صحيح

### 3. افتح صفحة الحضور
- من الشاشة الرئيسية → اضغط زر الحضور

### 4. تحقق من البيانات
- افتح console logs لمتابعة الطلبات
- تحقق من البيانات المسترجعة

---

## 📝 ملاحظات

- جميع الطلبات تتطلب `Authorization: Bearer {accessToken}`
- التواريخ يتم تحويلها تلقائياً من/إلى Date objects
- الصفحة تدعم RTL (Right-to-Left) للعربية
- جميع النصوص بالعربية
- Animations سلسة وجميلة
- بطاقات قابلة للتوسع والطي
- فلترة ديناميكية للجلسات

---

## ✅ تم الإنجاز

- ✅ إضافة API endpoint للحضور
- ✅ إنشاء أنواع TypeScript كاملة
- ✅ إنشاء attendance service
- ✅ إنشاء صفحة الحضور مع واجهة جميلة
- ✅ ربط الصفحة بالتنقل
- ✅ عرض الإحصائيات العامة
- ✅ عرض المواد الدراسية
- ✅ تفاصيل كل جلسة مع حالة الحضور
- ✅ ألوان مختلفة حسب حالة الحضور
- ✅ فلترة الجلسات حسب الحالة
- ✅ بطاقات قابلة للتوسع
- ✅ معالجة جميع الحالات (loading, error, empty)
- ✅ طابع أبيض فاتح ونظيف
- ✅ Animations وتجربة مستخدم ممتازة

---

تم بحمد الله! 🎉

**جرب الآن!** 🚀 افتح التطبيق واضغط على زر الحضور لرؤية صفحة الحضور الجديدة!
