# 📅 Schedule Slot Details - تفاصيل الفترة الدراسية

## 🎯 **الميزة الجديدة**

تم إضافة ميزة لعرض تفاصيل فترة دراسية واحدة من الجدول الدراسي مع جميع المعلومات المتعلقة بها.

## 🚀 **المميزات**

### ✅ **API Integration**
- **Endpoint**: `GET /api/schedule/slots/{id}`
- **Authentication**: JWT Token required
- **Response**: تفاصيل كاملة للفترة الدراسية

### ✅ **UI Components**
- **ScheduleSlotDetails**: مكون لعرض تفاصيل الفترة
- **تصميم متجاوب**: يعمل على جميع أحجام الشاشات
- **ألوان متدرجة**: حسب نوع الجلسة وحالتها

### ✅ **المعلومات المعروضة**
- **معلومات الفترة**: اليوم، الوقت، النوع، المكان
- **معلومات المادة**: الاسم، الكود، المدرب
- **معلومات الفصل**: الاسم، تواريخ البداية والنهاية
- **قائمة الجلسات**: جميع الجلسات مع تواريخها وحالتها

## 📱 **كيفية الاستخدام**

### **1. من الجدول الدراسي**
```typescript
// عند الضغط على جلسة في الجدول
const handleSessionPress = (session: ScheduleSession) => {
  setSelectedSlotId(session.id);
  setShowSlotDetails(true);
};
```

### **2. استخدام المكون مباشرة**
```typescript
<ScheduleSlotDetails
  slotId={123}
  accessToken={accessToken}
  onBack={() => navigation.goBack()}
/>
```

## 🎨 **التصميم**

- **خلفية فاتحة**: `#F8FAFC` (شبه أبيض)
- **بطاقات بيضاء**: مع ظلال بنفسجية ناعمة
- **ألوان مميزة**: للجلسات المجدولة والملغاة
- **خطوط واضحة**: سهلة القراءة بالعربية

## 📊 **مثال على البيانات**

```json
{
  "id": 1,
  "content": {
    "name": "مقدمة في البرمجة",
    "code": "CS101",
    "instructor": { "name": "أستاذ أحمد محمد" }
  },
  "dayOfWeek": "MONDAY",
  "startTime": "09:00",
  "endTime": "11:00",
  "type": "THEORY",
  "classroom": {
    "name": "الفصل الأول",
    "startDate": "2024-01-15",
    "endDate": "2024-06-15"
  },
  "sessions": [
    {
      "id": 1,
      "date": "2024-01-15",
      "isCancelled": false,
      "cancellationReason": null
    }
  ]
}
```

## 🔧 **الملفات المضافة/المحدثة**

### **Types**
- `src/types/auth.ts` - إضافة `ScheduleSlotResponse` و `Session`

### **Services**
- `src/services/apiConfig.ts` - إضافة `SCHEDULE_SLOT` endpoint
- `src/services/authService.ts` - إضافة `getScheduleSlot` method

### **Components**
- `src/components/ScheduleSlotDetails.tsx` - مكون جديد لعرض التفاصيل

### **Screens**
- `src/screens/ScheduleScreen.tsx` - تحديث للتنقل إلى التفاصيل

## ✅ **جاهز للاستخدام**

الميزة مكتملة وجاهزة للاستخدام! يمكن الآن:
- الضغط على أي جلسة في الجدول لعرض تفاصيلها
- رؤية جميع المعلومات المتعلقة بالفترة الدراسية
- عرض قائمة الجلسات مع حالتها (مجدولة/ملغاة)

🎉 **استمتع بالميزة الجديدة!**
