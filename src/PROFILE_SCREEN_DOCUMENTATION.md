# صفحة البروفايل - منصة المتدربين

## 🎯 نظرة عامة

تم إنشاء صفحة البروفايل الخاصة بالطالب مع تصميم عربي كامل وتكامل مع API endpoint `/api/trainee-auth/profile`.

## ✨ الميزات المُنجزة

### 🔧 **التكامل مع API**
- **Endpoint**: `/api/trainee-auth/profile`
- **Method**: GET
- **Authentication**: Bearer Token
- **Response**: بيانات البروفايل الكاملة مع البيانات المترابطة

### 🎨 **التصميم والواجهة**
- **تصميم عربي RTL** كامل
- **خلفية داكنة أنيقة** مع تأثيرات بصرية
- **أنيميشن سلس** للعناصر
- **بطاقات زجاجية** لعرض المعلومات
- **ألوان متناسقة** مع نظام التطبيق

### 📱 **المكونات الرئيسية**

#### **1. Header Section**
- **زر العودة** للصفحة الرئيسية
- **عنوان الصفحة** "الملف الشخصي"
- **زر التعديل** (جاهز للتطوير المستقبلي)

#### **2. Profile Card**
- **الصورة الشخصية** أو الحرف الأول من الاسم
- **الاسم بالعربية والإنجليزية**
- **حالة المتدرب** مع لون مميز
- **تأثيرات بصرية** متقدمة

#### **3. Quick Actions**
- **الوثائق** - عرض الوثائق المرفوعة
- **المدفوعات** - سجل المدفوعات
- **الحضور** - سجل الحضور والغياب
- **تعديل** - تعديل بيانات البروفايل

#### **4. Information Sections**
- **المعلومات الشخصية** - البيانات الأساسية
- **معلومات الاتصال** - أرقام الهواتف والعناوين
- **معلومات البرنامج** - تفاصيل البرنامج التدريبي
- **معلومات ولي الأمر** - بيانات ولي الأمر

## 🏗️ **الهيكل التقني**

### **الملفات المُنشأة/المُحدثة:**

#### **1. Types (src/types/auth.ts)**
```typescript
// إضافة الـ enums الجديدة
export enum FeeType { ... }
export enum PaymentStatus { ... }
export enum DocumentType { ... }
export enum AttendanceStatus { ... }
export enum SessionType { ... }

// إضافة الـ interfaces الجديدة
export interface TrainingContent { ... }
export interface Session { ... }
export interface AttendanceRecord { ... }
export interface TraineeFee { ... }
export interface TraineePayment { ... }
export interface TraineeDocument { ... }
export interface TraineeAuth { ... }
export interface TraineeProfileResponse { ... }
export type TraineeProfile = TraineeProfileResponse;
```

#### **2. API Configuration (src/services/apiConfig.ts)**
```typescript
export const API_CONFIG = {
  BASE_URL: 'http://10.0.2.2:4000',
  ENDPOINTS: {
    TRAINEE_LOGIN: '/api/trainee-auth/login',
    TRAINEE_PROFILE: '/api/trainee-auth/profile', // جديد
  },
  TIMEOUT: 10000,
} as const;
```

#### **3. Auth Service (src/services/authService.ts)**
```typescript
static async getProfile(accessToken: string): Promise<TraineeProfileResponse> {
  const url = `${API_CONFIG.BASE_URL}${API_CONFIG.ENDPOINTS.TRAINEE_PROFILE}`;
  
  return this.makeRequest<TraineeProfileResponse>(url, {
    method: 'GET',
    headers: {
      'Authorization': `Bearer ${accessToken}`,
    },
  });
}
```

#### **4. Profile Screen (src/screens/ProfileScreen.tsx)**
- **مكون كامل** لعرض البروفايل
- **معالجة الأخطاء** الشاملة
- **حالات التحميل** والخطأ
- **أنيميشن متقدم** للعناصر

#### **5. Navigation (src/navigation/AppNavigator.tsx)**
```typescript
type Screen = 'login' | 'home' | 'profile';

// إضافة التنقل بين الصفحات
const handleNavigateToProfile = () => {
  setCurrentScreen('profile');
};

const handleBackToHome = () => {
  setCurrentScreen('home');
};
```

#### **6. Home Screen (src/screens/HomeScreen.tsx)**
```typescript
interface HomeScreenProps {
  // ... existing props
  onNavigateToProfile?: () => void; // جديد
}

const handleProfile = () => {
  if (onNavigateToProfile) {
    onNavigateToProfile();
  }
};
```

## 🎨 **التصميم والأنيميشن**

### **الألوان المستخدمة:**
- **Primary**: `#6366F1` (Indigo)
- **Background**: `#0F172A` (Dark Navy)
- **Glass Effect**: `rgba(255, 255, 255, 0.98)`
- **Status Colors**: Success, Error, Warning, Info

### **الأنيميشن:**
- **Fade In**: ظهور تدريجي للعناصر
- **Slide Up**: حركة من الأسفل للأعلى
- **Scale**: تكبير تدريجي للبطاقات
- **Staggered Timing**: توقيت متدرج للعناصر

### **التخطيط:**
- **ScrollView** للتمرير السلس
- **SafeAreaView** للشاشات الآمنة
- **Responsive Design** لجميع أحجام الشاشات
- **RTL Support** كامل للعربية

## 📊 **عرض البيانات**

### **المعلومات الشخصية:**
- الرقم القومي
- تاريخ الميلاد
- النوع (ذكر/أنثى)
- الجنسية
- الحالة الاجتماعية

### **معلومات الاتصال:**
- رقم الهاتف
- البريد الإلكتروني
- العنوان
- المدينة والمحافظة

### **معلومات البرنامج:**
- اسم البرنامج
- نوع البرنامج (صيفي/شتوي/عقد سنة)
- الفرقة الدراسية
- العام الدراسي

### **معلومات ولي الأمر:**
- اسم ولي الأمر
- رقم هاتف ولي الأمر
- صلة القرابة
- وظيفة ولي الأمر

## 🔄 **معالجة الأخطاء**

### **أنواع الأخطاء المُعالجة:**
1. **Network Errors** - أخطاء الاتصال
2. **Authentication Errors** - انتهاء صلاحية الجلسة
3. **Server Errors** - أخطاء الخادم
4. **Timeout Errors** - انتهاء مهلة الاتصال

### **رسائل الخطأ:**
- **401**: "انتهت صلاحية الجلسة. يرجى تسجيل الدخول مرة أخرى"
- **0**: رسالة الخطأ من API
- **Default**: "حدث خطأ أثناء تحميل بيانات البروفايل"

## 🚀 **الاستخدام**

### **التنقل:**
1. من الصفحة الرئيسية → اضغط على "الملف الشخصي"
2. في صفحة البروفايل → اضغط على "العودة" للرجوع

### **الإجراءات السريعة:**
- **الوثائق**: عرض الوثائق المرفوعة (قريباً)
- **المدفوعات**: سجل المدفوعات (قريباً)
- **الحضور**: سجل الحضور (قريباً)
- **تعديل**: تعديل بيانات البروفايل (قريباً)

## 🔮 **التطوير المستقبلي**

### **الميزات المطلوبة:**
- [ ] **تعديل البروفايل** - إمكانية تعديل البيانات
- [ ] **رفع الصورة** - تغيير الصورة الشخصية
- [ ] **عرض الوثائق** - صفحة مخصصة للوثائق
- [ ] **سجل المدفوعات** - تفاصيل المدفوعات
- [ ] **سجل الحضور** - تفاصيل الحضور والغياب
- [ ] **إشعارات** - إشعارات متعلقة بالبروفايل

### **التحسينات المقترحة:**
- [ ] **Offline Support** - عرض البيانات المحفوظة محلياً
- [ ] **Pull to Refresh** - تحديث البيانات بالسحب
- [ ] **Search & Filter** - البحث والتصفية في البيانات
- [ ] **Export Data** - تصدير بيانات البروفايل
- [ ] **Dark/Light Mode** - تبديل الوضع المظلم/المضيء

## 🎯 **الخلاصة**

تم إنشاء صفحة بروفايل متكاملة وشاملة تتضمن:

✅ **تكامل API كامل** مع endpoint البروفايل  
✅ **تصميم عربي أنيق** مع RTL support  
✅ **أنيميشن متقدم** وسلس  
✅ **معالجة أخطاء شاملة**  
✅ **عرض بيانات منظم** في أقسام واضحة  
✅ **إجراءات سريعة** للوصول للميزات  
✅ **تنقل سلس** بين الصفحات  
✅ **كود نظيف** يتبع مبادئ SOLID  

النتيجة: صفحة بروفايل احترافية ومتكاملة جاهزة للاستخدام والتطوير! 🚀
