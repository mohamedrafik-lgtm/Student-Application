# إعدادات API - منصة المتدربين

## 🔧 إعدادات الخادم

### 📡 **عنوان الخادم المُحدث:**
```typescript
export const API_CONFIG = {
  BASE_URL: 'http://10.0.2.2:4000', // Android Emulator host
  ENDPOINTS: {
    TRAINEE_LOGIN: '/api/trainee-auth/login',
  },
  TIMEOUT: 10000, // 10 seconds
} as const;
```

### 🌐 **تفاصيل الاتصال:**

#### **العنوان الكامل:**
- **Base URL**: `http://10.0.2.2:4000`
- **Login Endpoint**: `http://10.0.2.2:4000/api/trainee-auth/login`
- **Port**: `4000`
- **Protocol**: `HTTP`

#### **لماذا 10.0.2.2؟**
- **Android Emulator**: يستخدم `10.0.2.2` للوصول إلى localhost
- **localhost**: `127.0.0.1` لا يعمل في Android Emulator
- **Host Machine**: `10.0.2.2` يشير إلى جهاز المضيف

### 🔧 **إعدادات إضافية:**

#### **Timeout:**
- **10 ثوان** للاستجابة القصوى
- **مناسب للشبكات البطيئة**
- **منع التعليق الطويل**

#### **Headers:**
```typescript
headers: {
  'Content-Type': 'application/json',
  ...options.headers,
}
```

### 📱 **اختبار الاتصال:**

#### **للأجهزة المختلفة:**
- **Android Emulator**: `http://10.0.2.2:4000`
- **iOS Simulator**: `http://localhost:4000`
- **جهاز حقيقي**: `http://[IP_ADDRESS]:4000`

#### **للتحقق من الاتصال:**
1. تأكد من تشغيل الخادم على Port 4000
2. تحقق من إمكانية الوصول من المتصفح
3. اختبر API endpoint مباشرة

### 🚀 **الاستخدام:**

#### **في AuthService:**
```typescript
const url = `${API_CONFIG.BASE_URL}${API_CONFIG.ENDPOINTS.TRAINEE_LOGIN}`;
// Result: http://10.0.2.2:4000/api/trainee-auth/login
```

#### **في المستقبل:**
- يمكن إضافة endpoints جديدة
- يمكن تغيير BASE_URL حسب البيئة
- يمكن إضافة authentication headers

### ⚠️ **ملاحظات مهمة:**

#### **الأمان:**
- **HTTP فقط** للتطوير
- **HTTPS مطلوب** للإنتاج
- **تشفير البيانات** الحساسة

#### **الشبكة:**
- **تأكد من تشغيل الخادم** على Port 4000
- **تحقق من firewall** settings
- **اختبر الاتصال** قبل التطوير

### 🎯 **الخلاصة:**

تم تحديث إعدادات API لتعمل مع:
- **Android Emulator** باستخدام `10.0.2.2:4000`
- **Timeout مناسب** للشبكات البطيئة
- **Headers صحيحة** للـ JSON
- **إعدادات مرنة** للتطوير المستقبلي

النتيجة: اتصال API يعمل بشكل صحيح مع Android Emulator! 🚀

