# ميزة عرض الدرجات للطالب 📊

## نظرة عامة

تم إضافة ميزة عرض الدرجات بشكل كامل مع واجهة مستخدم جميلة ومتكاملة مع الـ API. تعرض الصفحة الإحصائيات العامة والفصول الدراسية مع تفاصيل كل مادة.

---

## 🎯 الملفات المضافة/المعدلة

### 1. **الملفات الجديدة**
- `src/types/grades.ts` - أنواع TypeScript للدرجات
- `src/services/gradesService.ts` - خدمة API للدرجات
- `src/screens/GradesScreen.tsx` - صفحة عرض الدرجات

### 2. **الملفات المعدلة**
- `src/services/apiConfig.ts` - إضافة endpoint الدرجات
- `src/screens/HomeScreen.tsx` - ربط زر الدرجات
- `src/navigation/AppNavigator.tsx` - ربط صفحة الدرجات بالتنقل

---

## 🔗 API Endpoint المستخدم

```typescript
MY_GRADES: '/api/trainee-auth/my-grades'
```

---

## 📊 بنية البيانات

### **Response Structure**
```typescript
{
  trainee: {
    id: number;
    nameAr: string;           // الاسم بالعربية
    nameEn: string;           // الاسم بالإنجليزية
    nationalId: string;       // الرقم القومي
    program: {
      id: number;
      nameAr: string;         // اسم البرنامج بالعربية
      nameEn: string;         // اسم البرنامج بالإنجليزية
    };
  };
  
  overallStats: {
    totalEarned: number;      // إجمالي الدرجات المحصلة
    totalMax: number;         // إجمالي الدرجات الكاملة
    percentage: number;       // النسبة المئوية الإجمالية
    totalContents: number;    // إجمالي عدد المواد
  };
  
  classrooms: Array<{
    classroom: {
      id: number;
      name: string;           // اسم الفصل الدراسي
    };
    
    contents: Array<{
      content: {
        id: number;
        code: string;         // كود المادة
        name: string;         // اسم المادة
        yearWorkMarks: number;    // درجات أعمال السنة
        practicalMarks: number;   // درجات العملي
        writtenMarks: number;     // درجات التحريري
        attendanceMarks: number;  // درجات الحضور
        quizzesMarks: number;     // درجات اختبارات مصغرة
        finalExamMarks: number;   // درجات الميد تيرم
      };
      
      grades: {
        yearWorkMarks: number;    // درجات أعمال السنة المحصلة
        practicalMarks: number;   // درجات العملي المحصلة
        writtenMarks: number;     // درجات التحريري المحصلة
        attendanceMarks: number;  // درجات الحضور المحصلة
        quizzesMarks: number;     // درجات اختبارات مصغرة المحصلة
        finalExamMarks: number;   // درجات الميد تيرم المحصلة
        totalMarks: number;       // إجمالي الدرجات المحصلة
      };
      
      maxMarks: {
        yearWorkMarks: number;    // أقصى درجات أعمال السنة
        practicalMarks: number;   // أقصى درجات العملي
        writtenMarks: number;     // أقصى درجات التحريري
        attendanceMarks: number;  // أقصى درجات الحضور
        quizzesMarks: number;     // أقصى درجات اختبارات مصغرة
        finalExamMarks: number;   // أقصى درجات الميد تيرم
        total: number;            // إجمالي أقصى الدرجات
      };
      
      percentage: number;         // النسبة المئوية للمادة
    }>;
    
    stats: {
      totalEarned: number;        // إجمالي الدرجات المحصلة في الفصل
      totalMax: number;           // إجمالي الدرجات الكاملة في الفصل
      percentage: number;         // النسبة المئوية للفصل
      contentCount: number;       // عدد المواد في الفصل
    };
  }>;
}
```

---

## 🎨 واجهة المستخدم

### **الميزات الرئيسية**

#### 1️⃣ **الإحصائيات العامة**
```
┌─────────────────────────────────┐
│        الإحصائيات العامة        │
│         اسم الطالب              │
├─────────────────────────────────┤
│  85.5%    │  425/500   │   12   │
│ النسبة    │ إجمالي     │ عدد    │
│ الإجمالية │ الدرجات    │ المواد │
├─────────────────────────────────┤
│           جيد جداً              │
└─────────────────────────────────┘
```

#### 2️⃣ **الفصول الدراسية**
```
┌─────────────────────────────────┐
│        الفصل الدراسي الأول      │
│        5 مادة • 87.2%           │
│                        87.2%    │
│                        425/500  │
├─────────────────────────────────┤
│  MATH101  │  الرياضيات  │  90%  │
│  PHYS201  │  الفيزياء   │  85%  │
│  CHEM301  │  الكيمياء   │  88%  │
└─────────────────────────────────┘
```

#### 3️⃣ **تفاصيل المادة (قابلة للتوسع)**
```
┌─────────────────────────────────┐
│  MATH101 - الرياضيات     90.0%  │
│  425/500                        │
│  ممتاز                    ▼     │
├─────────────────────────────────┤
│  تفاصيل الدرجات:                │
│  📝 أعمال السنة    85/100  85%  │
│  🔬 العملي         45/50   90%  │
│  ✍️ التحريري       180/200 90%  │
│  📅 الحضور         20/20   100% │
│  📋 اختبارات مصغرة 45/50   90%  │
│  🎯 الميد تيرم     50/80   62%  │
└─────────────────────────────────┘
```

---

## 🎯 أنواع الدرجات

### **Grade Types**
```typescript
enum GradeType {
  YEAR_WORK = 'yearWorkMarks',     // أعمال السنة
  PRACTICAL = 'practicalMarks',    // العملي
  WRITTEN = 'writtenMarks',        // التحريري
  ATTENDANCE = 'attendanceMarks',  // الحضور
  QUIZZES = 'quizzesMarks',        // اختبارات مصغرة
  FINAL_EXAM = 'finalExamMarks',   // الميد تيرم
}
```

### **ألوان الدرجات**
- **90%+**: أخضر `#10B981` (ممتاز)
- **80-89%**: أزرق `#3B82F6` (جيد جداً)
- **70-79%**: برتقالي `#F59E0B` (جيد)
- **60-69%**: أحمر `#EF4444` (مقبول)
- **أقل من 60%**: رمادي `#6B7280` (راسب)

---

## 🚀 كيفية الاستخدام

### 1. الوصول لصفحة الدرجات
من الشاشة الرئيسية → اضغط على زر "الدرجات" 📊

### 2. عرض الدرجات
- تظهر الإحصائيات العامة في الأعلى
- قائمة بالفصول الدراسية
- كل فصل يحتوي على المواد

### 3. تفاصيل المادة
1. اضغط على أي مادة لرؤية التفاصيل
2. ستظهر تفاصيل جميع أنواع الدرجات
3. النسب المئوية لكل نوع
4. الدرجات المحصلة/الكلية

---

## 📱 حالات الشاشة

### Loading
- عرض spinner مع رسالة "جاري تحميل الدرجات..."

### Error
- عرض emoji تحذير
- رسالة الخطأ
- زر "إعادة المحاولة"

### Empty
- عرض emoji 📊
- رسالة "لا توجد درجات متاحة"

### Success
- عرض الإحصائيات العامة
- قائمة بالفصول الدراسية
- تفاصيل كل مادة

---

## 🎨 نظام الألوان

### **الخلفيات**
```css
--bg-primary: #FFFFFF;      /* خلفية رئيسية */
--bg-secondary: #F9FAFB;    /* خلفية ثانوية */
--bg-tertiary: #F3F4F6;     /* خلفية أزرار */
```

### **النصوص**
```css
--text-primary: #1F2937;    /* نصوص رئيسية */
--text-secondary: #6B7280;  /* نصوص ثانوية */
--text-error: #EF4444;      /* نصوص خطأ */
```

### **الحدود**
```css
--border-light: #E5E7EB;    /* حدود فاتحة */
--border-primary: #3B82F6;  /* حدود نشطة */
```

### **الدرجات**
```css
--grade-excellent: #10B981; /* ممتاز */
--grade-very-good: #3B82F6; /* جيد جداً */
--grade-good: #F59E0B;      /* جيد */
--grade-acceptable: #EF4444; /* مقبول */
--grade-fail: #6B7280;      /* راسب */
```

---

## 🔄 تدفق البيانات

```
1. المستخدم يفتح صفحة الدرجات
   ↓
2. GradesScreen.tsx → loadGrades()
   ↓
3. gradesService.getMyGrades(accessToken)
   ↓
4. API Request → /api/trainee-auth/my-grades
   ↓
5. Response → MyGradesResponse
   ↓
6. تحديث state → setGradesData(response.data)
   ↓
7. عرض البيانات في البطاقات
```

---

## 🛠️ SOLID Principles

الكود يتبع مبادئ SOLID:
- **Single Responsibility**: كل ملف له مسؤولية واحدة
- **Open/Closed**: يمكن التوسع بدون تعديل الكود الموجود
- **Interface Segregation**: واجهات محددة لكل غرض
- **Dependency Inversion**: الاعتماد على Abstractions وليس Concretions

---

## 🧪 كيفية الاختبار

### 1. تأكد من تشغيل الخادم
```bash
# تأكد من أن API متاح على أحد الفروع
https://mansapi.tiba29.com
# أو
https://zagapi.tiba29.com
```

### 2. سجل الدخول
- استخدم حساب متدرب صحيح

### 3. افتح صفحة الدرجات
- من الشاشة الرئيسية → اضغط زر الدرجات

### 4. تحقق من البيانات
- افتح console logs لمتابعة الطلبات
- تحقق من البيانات المسترجعة

---

## 📝 ملاحظات

- جميع الطلبات تتطلب `Authorization: Bearer {accessToken}`
- التواريخ يتم تحويلها تلقائياً من/إلى Date objects
- الصفحة تدعم RTL (Right-to-Left) للعربية
- جميع النصوص بالعربية
- Animations سلسة وجميلة
- بطاقات قابلة للتوسع والطي

---

## ✅ تم الإنجاز

- ✅ إضافة API endpoint للدرجات
- ✅ إنشاء أنواع TypeScript كاملة
- ✅ إنشاء grades service
- ✅ إنشاء صفحة الدرجات مع واجهة جميلة
- ✅ ربط الصفحة بالتنقل
- ✅ عرض الإحصائيات العامة
- ✅ عرض الفصول الدراسية
- ✅ تفاصيل كل مادة مع أنواع الدرجات
- ✅ ألوان مختلفة حسب النسبة المئوية
- ✅ بطاقات قابلة للتوسع
- ✅ معالجة جميع الحالات (loading, error, empty)
- ✅ طابع أبيض فاتح ونظيف
- ✅ Animations وتجربة مستخدم ممتازة

---

تم بحمد الله! 🎉

**جرب الآن!** 🚀 افتح التطبيق واضغط على زر الدرجات لرؤية صفحة الدرجات الجديدة!
