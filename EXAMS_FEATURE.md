# ميزة الاختبارات الإلكترونية 📝

## نظرة عامة

تم إضافة ميزة الاختبارات الإلكترونية بشكل كامل مع واجهة مستخدم جميلة ومتكاملة مع الـ API.

---

## 🎯 الملفات المضافة/المعدلة

### 1. **الملفات الجديدة**
- `src/types/quizzes.ts` - أنواع TypeScript للاختبارات
- `src/services/quizService.ts` - خدمة API للاختبارات
- `src/screens/ExamsScreen.tsx` - صفحة عرض الاختبارات

### 2. **الملفات المعدلة**
- `src/services/apiConfig.ts` - إضافة endpoints الاختبارات
- `src/screens/HomeScreen.tsx` - إضافة زر الاختبارات
- `src/navigation/AppNavigator.tsx` - ربط صفحة الاختبارات بالتنقل

---

## 🔗 API Endpoints المستخدمة

```typescript
AVAILABLE_QUIZZES: '/api/quizzes/trainee/available'  // قائمة الاختبارات المتاحة
QUIZ_DETAIL: '/api/quizzes/{quizId}'                 // تفاصيل اختبار معين
START_QUIZ: '/api/quizzes/{quizId}/start'            // بدء اختبار
SUBMIT_QUIZ: '/api/quizzes/{quizId}/submit'          // إرسال إجابات
QUIZ_RESULT: '/api/quizzes/{quizId}/result'          // الحصول على النتيجة
```

---

## 📊 أنواع البيانات

### حالات الاختبار (QuizStatus)
- `available` - متاح للمتدرب
- `upcoming` - لم يبدأ بعد
- `completed` - أنهاه المتدرب
- `ended` - انتهى موعده

### بنية بيانات الاختبار (AvailableQuiz)
```typescript
{
  id: number,
  title: string,
  description: string | null,
  duration: number,              // بالدقائق
  passingScore: number,          // نسبة مئوية
  maxAttempts: number,
  startDate: Date,
  endDate: Date,
  trainingContent: {
    id: number,
    name: string,
    code: string,
    classroom: { id, name }
  },
  _count: { questions: number },
  status: QuizStatus,
  canAttempt: boolean,
  isCompleted: boolean,
  hasInProgress: boolean,
  result: {
    id: string,
    score: number,
    percentage: number,
    passed: boolean,
    submittedAt: Date
  } | null
}
```

---

## 🎨 واجهة المستخدم

### الميزات
1. **فلترة الاختبارات**
   - الكل
   - متاح
   - مكتمل
   - قريباً

2. **بطاقة الاختبار تعرض:**
   - عنوان الاختبار
   - حالة الاختبار (متاح/مكتمل/قريباً/منتهي)
   - اسم المقرر والكود
   - مدة الاختبار
   - عدد الأسئلة
   - نسبة النجاح المطلوبة
   - تاريخ البداية والنهاية
   - النتيجة (إذا كان مكتملاً)

3. **التفاعل**
   - الضغط على اختبار متاح → تأكيد البدء
   - الضغط على اختبار مكتمل → عرض النتيجة
   - الضغط على اختبار غير متاح → رسالة توضيحية

---

## 🚀 كيفية الاستخدام

### 1. الوصول لصفحة الاختبارات
من الشاشة الرئيسية → اضغط على زر "الاختبارات" 📝

### 2. عرض الاختبارات
- تظهر جميع الاختبارات المتاحة تلقائياً
- استخدم الفلاتر للتصفية حسب الحالة

### 3. بدء اختبار
1. اضغط على بطاقة اختبار متاح
2. اقرأ المعلومات في نافذة التأكيد
3. اضغط "بدء الاختبار"
4. (قريباً) سيتم فتح صفحة الاختبار

---

## 📱 حالات الشاشة

### Loading
- عرض spinner مع رسالة "جاري تحميل الاختبارات..."

### Error
- عرض emoji تحذير
- رسالة الخطأ
- زر "إعادة المحاولة"

### Empty
- عرض emoji 📝
- رسالة "لا توجد اختبارات متاحة"

### Success
- عرض قائمة الاختبارات
- كل اختبار في بطاقة مميزة
- ألوان مختلفة حسب الحالة

---

## 🎨 نظام الألوان

```typescript
AVAILABLE (متاح)   → Colors.success (أخضر)
COMPLETED (مكتمل)  → Colors.info (أزرق)
UPCOMING (قريباً)  → Colors.warning (برتقالي)
ENDED (منتهي)      → Colors.error (أحمر)
```

---

## 🔄 تدفق البيانات

```
1. المستخدم يفتح صفحة الاختبارات
   ↓
2. ExamsScreen.tsx → loadQuizzes()
   ↓
3. quizService.getAvailableQuizzes(accessToken)
   ↓
4. API Request → /api/quizzes/trainee/available
   ↓
5. Response → AvailableQuizzesResponse
   ↓
6. تحديث state → setQuizzes(response.quizzes)
   ↓
7. عرض البيانات في البطاقات
```

---

## 🔜 الخطوات القادمة

### المطلوب إضافته لاحقاً:
1. **صفحة تقديم الاختبار** (`QuizTakingScreen.tsx`)
   - عرض الأسئلة
   - مؤقت عد تنازلي
   - حفظ الإجابات
   - إرسال الإجابات

2. **صفحة النتائج** (`QuizResultScreen.tsx`)
   - عرض الدرجة
   - عرض الإجابات الصحيحة والخاطئة
   - إحصائيات

3. **التنبيهات والإشعارات**
   - تنبيه عند توفر اختبار جديد
   - تنبيه قبل انتهاء موعد الاختبار

---

## 🧪 كيفية الاختبار

### 1. تأكد من تشغيل الخادم
```bash
# تأكد من أن API متاح على أحد الفروع
https://mansapi.tiba29.com
# أو
https://zagapi.tiba29.com
```

### 2. سجل الدخول
- استخدم حساب متدرب صحيح

### 3. افتح صفحة الاختبارات
- من الشاشة الرئيسية → اضغط زر الاختبارات

### 4. تحقق من البيانات
- افتح console logs لمتابعة الطلبات
- تحقق من البيانات المسترجعة

---

## 📝 ملاحظات

- جميع الطلبات تتطلب `Authorization: Bearer {accessToken}`
- التواريخ يتم تحويلها تلقائياً من/إلى Date objects
- الصفحة تدعم RTL (Right-to-Left) للعربية
- جميع النصوص بالعربية
- Animations سلسة وجميلة

---

## 🛠️ SOLID Principles

الكود يتبع مبادئ SOLID:
- **Single Responsibility**: كل ملف له مسؤولية واحدة
- **Open/Closed**: يمكن التوسع بدون تعديل الكود الموجود
- **Interface Segregation**: واجهات محددة لكل غرض
- **Dependency Inversion**: الاعتماد على Abstractions وليس Concretions

---

## ✅ تم الإنجاز

- ✅ إضافة زر الاختبارات في الشاشة الرئيسية
- ✅ إنشاء صفحة الاختبارات
- ✅ ربط الصفحة بالتنقل
- ✅ إضافة أنواع TypeScript
- ✅ إضافة API endpoints
- ✅ إنشاء quiz service
- ✅ واجهة مستخدم كاملة مع الفلاتر
- ✅ دعم جميع حالات الاختبار
- ✅ عرض النتائج للاختبارات المكتملة
- ✅ Animations وتجربة مستخدم ممتازة

---

تم بحمد الله! 🎉

